import assert from 'power-assert';
import Throw from '../src/throw';

describe('Throw', () => {
    it('does something useful', () => {
        var itsThrow = new Throw;
        assert(itsThrow == itsThrow);
    })
})
