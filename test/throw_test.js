/**
 * Created by k2works on 2017/02/15.
 */

import assert from 'power-assert';
import Throw from '../src/lib/throw';

describe('Throw', () => {
    it('does something useful', () => {
        var obj = new Throw;
        assert(obj === obj);
    });
});